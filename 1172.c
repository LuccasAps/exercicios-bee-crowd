#include <stdio.h>
#include <math.h>
int main()
{
    int x,i,ql,a,b;
    scanf("%d",&x);
    ql = pow(2, x);
    int [ql][x];
    for (i = 0; i < ql; i++)
    {
        for (j = 0; j < x; j++)
        {
            
        }
        
    }
    
    return 0;
}